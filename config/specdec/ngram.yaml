name: ngram
method: ngram
speculative_config:
  method: ngram
  num_speculative_tokens: null    # frozen at W1 from the installed version's spec-decode docs
  prompt_lookup_max: null
applies_to: [qwen3_8b_default_precision, qwen3_4b]
